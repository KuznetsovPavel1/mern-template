import React from "react";

import { labels } from "../structures/langRU";
const { errPageNotFound } = labels;

const NotFound = () => (
  <div>
    <h4>{errPageNotFound}</h4>
  </div>
);

export default NotFound;
